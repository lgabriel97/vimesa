# Base de datos
DB_USER=vimesa
# generar con pwgen 32 1
DB_PASSWORD=cambia-esto-por-algo-largo
DB_NAME=verificaciones

# JWT — generar con: openssl rand -base64 48
JWT_SECRET=cambia-esto-por-un-secreto-aleatorio-de-al-menos-48-caracteres